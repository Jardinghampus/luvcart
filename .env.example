AUTH_SECRET=change-me-to-a-long-random-string
SLIDESHOW_PASSWORD=Gamlastan24
# Optional — when set, photos upload to Vercel Blob + slideshow lists them.
# Without it, photos save locally to /public/uploads (great for localhost).
# BLOB_READ_WRITE_TOKEN=
